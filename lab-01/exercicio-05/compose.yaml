services:
  redis:
    image: redis:alpine

  webapp:
    build: .
    volumes:
      - ./app:/app
    ports:
      - 5000
    deploy:
      replicas: 3
  caddy:
    image: caddy:alpine
    ports:
      - 4000:4000
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile